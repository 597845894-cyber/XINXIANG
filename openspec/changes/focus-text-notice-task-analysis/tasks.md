## 1. 文字首版范围收敛

- [ ] 1.1 移除快速导入、收件箱和桌面入口中的图片选择、剪贴板图片、预览、OCR 修正与相关提示，并用前端交互测试验证界面只提供文字导入
- [ ] 1.2 将图片导入本地命令改为稳定的“不支持图片导入”结果，确保请求不会读取或保存图片，并用 Rust 测试验证无通知和附件残留
- [ ] 1.3 停用 OCR 分析路径、OCR 模型检查和图片端到端验收入口，同时保留既有图片记录不被自动删除，并通过现有迁移/读取测试验证文字通知与已确认任务不受影响
- [ ] 1.4 更新模型资源、用户说明和发布范围，明确首版仅处理文字且模型资源位于用户配置的本地目录，并通过文档和资源清单检查验证不包含 OCR 权重或联网下载逻辑

## 2. 本地千问运行时

- [ ] 2.1 实现 E 盘本地 Qwen GGUF 文件与 Windows CPU 运行时的路径配置、版本/哈希校验和启动健康检查，并用缺失、损坏、可用资源测试验证不会访问网络或复制模型到 C 盘
- [ ] 2.2 实现单并发、可超时和可取消的本地推理进程适配器，向 Qwen 传入文字通知并读取单一结构化响应，并用模拟运行时测试验证成功、超时、取消和崩溃回收
- [ ] 2.3 定义版本化的受约束分析 JSON 合同与固定提示词，覆盖分类、变更意图及多个候选的标题、字段、可信状态和原文证据，并用合同测试验证无效 JSON 和未知字段被拒绝
- [ ] 2.4 将模型结果接入现有字段验证、日期解析和分析版本原子保存；验证任一校验失败不会留下候选或正式任务，且规则回退结果明确标为低可信

## 3. 任务拆分与人工核对

- [ ] 3.1 更新文字通知分析界面，显示本地模型状态、分析进度、通知类别、拆分出的独立候选及每项原文依据，并用前端交互测试验证多个候选可分别进入核对
- [ ] 3.2 对缺失、冲突、低可信、模型不可用和分析失败状态提供重试、规则回退或人工创建任务入口，并验证任何恢复路径均不覆盖原文或已确认任务
- [ ] 3.3 为报名、材料提交、活动参与三阶段通知建立端到端领域测试，验证生成三个候选，且分别确认后只创建对应的正式任务和审计记录

## 4. 文字版质量与发布验收

- [ ] 4.1 将评测集收敛为合成文字通知，覆盖单任务、多任务、多阶段、缺失字段、相对日期、冲突、改期、取消和仅供知晓，并生成可复现的分类、拆分、字段、证据合规率与耗时报告
- [ ] 4.2 在断网的 Windows 主机执行文字导入、本地千问分析、候选确认、搜索、提醒、备份恢复完整流程，验证不需要登录、许可或远程依赖，并记录测试结果
- [ ] 4.3 在 8 GB、CPU-only 基准机记录典型千字以内文字通知的峰值内存、界面响应与分析耗时，验证超过目标时可取消且不会阻塞界面
- [ ] 4.4 构建不含模型权重的 Windows 文字版安装包，由用户验证本地模型目录配置、离线启动、托盘、升级/回滚和卸载，并将每个可验收阶段的代码、测试和说明提交为独立 Git 提交
