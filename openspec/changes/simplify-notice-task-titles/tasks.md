## 1. 合同与分析规则

- [x] 1.1 在实施前更新或标记 `improve-notice-task-decomposition` 中要求 8 个动作的 fixture、测试和验收标准，确保仓库只保留一套生效的任务数量门槛；通过 OpenSpec 状态和离线评测检查确认无冲突标准
- [x] 1.2 为候选结果补充主要任务摘要、聚合键、详情动作、时间概括和待确认字段，并保留旧候选兼容读取；通过 Rust/TypeScript 合同测试验证新旧 JSON 均可解析
- [x] 1.3 更新本地千问生产提示词与评测提示词，明确平台/入口、截止时间、提交对象或独立确认目标不同才拆分；通过提示词版本一致性检查验证资源同步
- [x] 1.4 更新规则回退和结果归一化逻辑，按确定性优先级聚合动作，保留连续证据、材料与条件，并限制默认标题不超过 32 个中文字符；通过单元测试验证无证据候选被拒绝且不会生成空标题
- [x] 1.5 统一时间概括与聚合作用域，支持共享截止时间、模糊时间待确认和单项覆盖；通过时间边界测试验证不猜测“上课前”的具体时刻
- [x] 1.6 增加本地 `task_summary_mode` 配置，默认 `aggregated`，支持 `flat_legacy` 回退且不覆盖历史数据；通过配置切换测试验证两种模式均可启动

## 2. 领域回归与质量门槛

- [x] 2.1 将实习通知 fixture 的期望固定为两项主要候选，并验证详情覆盖校友邦、纸质材料、手册评语、盖章、拍照和装袋说明
- [x] 2.2 增加不同截止时间、不同提交对象、单任务和无日期通知样本；通过离线语义评测验证不应合并的事项仍能拆分
- [x] 2.3 扩展评测输出聚合准确率、主要候选数量、背景误报率、证据合规率和时间待确认率；生成可复查的本地报告

## 3. 候选核对界面

- [x] 3.1 将分析结果卡片改为响应式多行布局，确保标题、时间、状态、详情和证据在窄窗口自然换行；通过前端测试或 Windows 窄窗口人工检查验证中文不竖排、不重叠
- [x] 3.2 展示聚合说明、包含的动作/材料和待确认原因，并保留编辑、合并、拆分、忽略和人工创建入口；通过交互测试验证拆分后候选可分别确认
- [x] 3.3 兼容展示旧扁平候选，将其视为独立任务并隐藏不存在的聚合字段；通过加载历史分析样本验证页面不报错

## 4. 任务表可见性与统计

- [x] 4.1 抽取统一任务筛选谓词，驱动今天、本周、即将截止、无日期、已完成和全部任务列表及统计数字；通过组件测试验证计数与列表条目始终一致
- [x] 4.2 确认候选后刷新任务查询并提供进入“全部任务”或相关视图的入口，确保无截止时间任务不被默认筛选隐藏；通过端到端交互测试验证新任务立即可见
- [x] 4.3 完善空状态文案和视图切换操作，区分当前筛选为空与任务尚未创建；通过空列表测试验证用户能定位已确认任务

## 5. 集成验收与回退

- [ ] 5.1 运行 Rust、前端、合同和离线语义测试，确认实习通知达到聚合数量、详情证据和时间概括门槛；记录测试结果（前端/合同/build 已通过，Rust 受 OpenSSL vendored 环境阻塞）
- [ ] 5.2 在断网 Windows 环境人工验收文字导入、分析、候选确认、任务表查看和历史回溯，确认通知数据不离开设备；交付验收记录（待用户人工验收）
- [x] 5.3 验证 `task_summary_mode=flat_legacy` 可回退旧扁平候选生成，且不会覆盖既有通知、分析版本或正式任务
