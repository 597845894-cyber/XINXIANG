## 1. 基线确认与历史审计

- [ ] 1.1 从干净且与 `origin/main` 一致的 `main` 创建 `change/establish-version-control-guardrails` 分支，记录起始提交 `f5b2238`，并验证 `git status`、上游分支和起始哈希符合预期
- [ ] 1.2 建立仓库敏感数据分类与禁止路径清单，覆盖通知、截图、数据库、密钥、备份、模型和诊断数据，并验证每类都映射到忽略规则或扫描规则
- [ ] 1.3 选择并锁定本地及 CI 使用的秘密扫描器版本、许可证和校验方式，在提交任何安全工具链前扫描当前索引和全部 refs，并验证报告无真实凭据、私有通知或超限二进制
- [ ] 1.4 为基线提交 `f5b2238` 创建带注释标签 `v0.1.0-plan.1` 并推送，验证本地 tag、远程 tag 和目标提交哈希完全一致

## 2. 敏感文件忽略规则

- [ ] 2.1 扩展 `.gitignore` 的凭据与签名材料规则，加入 secrets 目录、PFX/P12/JKS/keystore 和本地认证文件，并用 `git check-ignore -v` 验证每个代表路径由预期规则命中
- [ ] 2.2 扩展本地通知、私有截图、数据库旁路文件、恢复点、密钥包装、备份临时目录和崩溃转储规则，并验证代表性敏感文件不会出现在 `git status --untracked-files=all`
- [ ] 2.3 补齐模型缓存、下载临时文件和大模型扩展名规则，同时建立可提交的公开合成夹具目录约定，并验证公开小型夹具可跟踪、真实/私有夹具被忽略
- [ ] 2.4 检查当前已跟踪文件是否违反新规则；若有命中则停止自动处理并报告，若无命中则保存可复现的索引审计结果

## 3. 本地提交安全门禁

- [ ] 3.1 新增 PowerShell 仓库安全检查脚本，支持暂存区与完整历史模式，检查禁止路径、敏感扩展、常见凭据模式和文件大小，并用无害测试样本验证通过与拒绝结果
- [ ] 3.2 为安全检查脚本增加自动化测试，覆盖 Unicode 路径、重命名、删除、二进制、误报允许项和超过阈值文件，并验证测试在 Windows PowerShell 环境稳定通过
- [ ] 3.3 添加秘密扫描器配置和最小允许清单，要求允许项包含理由且不允许真实凭据值，并用模拟秘密验证扫描失败、普通源码验证通过
- [ ] 3.4 新增仓库管理的 pre-commit hook 和安装脚本，将 `core.hooksPath` 指向项目 hook，依次运行差异格式检查与敏感扫描，并验证失败会阻止提交且错误信息包含修复入口
- [ ] 3.5 提供 hook 安装状态检查和安全卸载说明，并验证重复安装幂等、缺少扫描器时失败关闭而不是跳过检查

## 4. 版本与开发指引

- [ ] 4.1 新增 `VERSIONING.md`，记录原子提交定义、Conventional Commits、首版标签路线、查询方式和基于 revert 的回退流程，并逐条试运行只读查询命令验证文档准确
- [ ] 4.2 新增 `CHANGELOG.md`，记录 `v0.1.0-plan.1` 的产品范围、技术基线、已知限制和提交哈希，并验证版本、tag 与 Git 历史一致
- [ ] 4.3 新增 `CONTRIBUTING.md`，说明 OpenSpec 分支、逐任务验证提交、及时推送、Pull Request 和敏感数据处理流程，并验证新开发者可按文档完成一次模拟流程
- [ ] 4.4 新增项目级 `AGENTS.md`，要求自动化代理修改前检查 Git、保护用户未提交修改、完成后验证、扫描、原子提交、推送并报告哈希，同时明确禁止改写共享历史
- [ ] 4.5 更新 `openspec/config.yaml` 的 apply guidance，把任务编号、相关验证、commit 哈希和远程推送纳入每个原子修改的完成定义，并验证 OpenSpec apply 指令能够读取这些约束
- [ ] 4.6 在 `build-local-campus-notice-inbox/tasks.md` 中加入各任务组对应的版本门禁和 tag 检查任务，不改变原有功能验收内容，并验证任务编号和依赖顺序仍然连续可解析

## 5. GitHub 远程门禁

- [ ] 5.1 新增 GitHub Actions 仓库安全工作流，在 Pull Request 和 `main` 推送时运行格式、禁止路径、大文件和秘密扫描，并验证 action 依赖固定到不可变版本或提交
- [ ] 5.2 新增 Pull Request 模板，要求填写 OpenSpec 变更、任务编号、验证结果、敏感扫描结果、commit 列表和回退方式，并验证模板字段覆盖版本完成定义
- [ ] 5.3 配置 GitHub `main` 分支规则，禁止强制推送和删除、要求安全检查通过并通过 Pull Request 合并，同时禁用 squash 作为本项目默认合并方式；读取远程规则验证配置生效
- [ ] 5.4 验证每个成功的原子 commit 推送后能从远程分支按哈希查询，并模拟网络暂不可用后恢复推送，确认本地提交顺序和远程提交顺序一致

## 6. 追溯与回退验收

- [ ] 6.1 在隔离临时仓库运行正常文件、忽略文件、强制添加敏感文件、模拟 token、大文件和完整历史扫描场景，验证三层门禁只允许安全提交
- [ ] 6.2 在隔离临时仓库创建两个原子修改和一个版本 tag，按照 `VERSIONING.md` 查询文件历史、任务编号、tag 和差异，验证均能定位到精确 commit
- [ ] 6.3 在隔离临时仓库对已推送模拟提交执行 revert 流程，验证旧提交仍可查询、工作内容被撤销且回退本身形成新 commit，不使用 reset 或强制推送
- [ ] 6.4 审计本治理变更的提交历史，验证每个完成的独立修改均有 Conventional Commit、OpenSpec 任务编号、验证记录和 GitHub 远程哈希
- [ ] 6.5 运行 OpenSpec 严格校验、仓库安全检查和 Git 状态检查，确认工作区干净、`main` 与远程一致后合并治理变更
- [ ] 6.6 为治理完成提交创建并推送 `v0.1.0-plan.2` 带注释标签，更新变更记录，并验证本地/远程标签、commit 和 GitHub 检查全部一致
