## 1. 合同与版本基础

- [x] 1.1 定义分析合同 v2 的任务图字段（稳定 ID、父任务、依赖、关系、条件、时间作用域和证据范围），同步 Rust/TypeScript 类型与 JSON Schema，并用合同测试验证未知字段、缺失字段和 v1 兼容读取
- [x] 1.2 将生产 system prompt 与 `benchmarks/semantic` prompt/schema 合并为同一版本资源，增加动作拆分、背景过滤、条件动作和证据连续片段规则，并用版本一致性测试阻止漂移
- [x] 1.3 为 v2 分析结果、候选和确认记录增加版本标识及审计字段，验证旧分析和已确认任务仍可读取且不会被重新分析覆盖

## 2. 动作级拆分与依赖构建

- [x] 2.1 在通知文本预处理阶段按编号、标点、并列连接词和条件词生成句段，并实现动作/说明句分类；用背景句、并列任务和长通知单元测试验证切分边界
- [x] 2.2 扩展规则回退动作词和动作宾语提取，至少覆盖完成、导出、联系、盖章、拍照、插入、装入、提交等词，并验证每个回退候选都绑定原文证据且没有空动作标题
- [x] 2.3 在模型结果归一化阶段校验证据连续性、动作覆盖、重复候选和编号项完整性；验证无证据候选被拒绝，遗漏项产生明确警告而非静默通过
- [x] 2.4 根据汇总动作、准备动作和条件词构建父子/前置/条件关系，输出稳定关系 ID；用实习通知 fixture 验证预期动作数量、关系方向和条件“无评语时联系指导老师”

## 3. 中文时间语义与本地化保存

- [x] 3.1 扩展日期表达抽取和解析，支持月日、星期、相对日期、“下午”“上课前”“之前完成”等表达，并为无法确定的精度输出 `needsReview`；用固定发布时间测试解析结果
- [x] 3.2 实现共享时间作用域继承与单项具体时间覆盖，验证列表项继承引导句截止时间、无关背景不继承、冲突时间进入待确认
- [x] 3.3 移除业务层强制 UTC `Z` 拼接，统一保存带偏移的本地时间或纯日期；用中国时区 8 月 31 日边界测试确认不跨日显示
- [x] 3.4 将时间原文、精度、解析状态和本地值接入候选合同及前端展示，验证模糊时间显示原文并阻止无提示快速确认

## 4. 候选核对与任务确认

- [x] 4.1 更新候选核对界面为树状或缩进列表，显示父任务、准备步骤、条件、共享截止时间、证据和待确认原因；用前端交互测试验证长标题和多层关系不重叠
- [x] 4.2 更新确认、编辑、忽略和拆分流程，使父任务与子步骤可分别确认并保持候选 ID 关联；验证只确认选中的候选，未确认步骤不生成正式任务
- [x] 4.3 为低可信、证据缺失、时间冲突、模型失败和规则回退增加明确恢复入口，验证重试/人工创建不会覆盖原文或已确认任务

## 5. 质量评测与发布验收

- [x] 5.1 增加脱敏真实通知回归集，包含本次实习通知和单任务、多任务、条件、共享时间、冲突时间样本，并为每条样本维护期望动作图
- [x] 5.2 扩展评测脚本输出动作召回率、背景误报率、依赖关系准确率、时间解析准确率、证据合规率和结构化通过率；验证报告可重复生成并标出高风险错误
- [ ] 5.3（由 `simplify-notice-task-titles` 接管）运行前端、Rust、合同和语义评测，确认实习通知达到两个主要候选及其详情/时间门槛
- [ ] 5.4 在断网 Windows 环境执行导入、分析、候选确认、提醒和备份恢复人工验收，验证通知数据不离开设备，并交付带版本号的安装包与回退说明
