# 第一阶段 Windows 交付检查

## 构建内容

- 应用：校园信箱 0.1.0
- 安装目标：NSIS
- 模型资源：`xinxiang.placeholder-model`，仅验证本地资源随包安装，不包含推理权重或用户数据
- 核心运行条件：无需登录、无需业务服务端、无需远程模型许可

## 验收记录

| 环境                 | 安装                                 | 断网启动           | 占位资源                         | 卸载       | 状态                                 |
| -------------------- | ------------------------------------ | ------------------ | -------------------------------- | ---------- | ------------------------------------ |
| 当前 Windows 11 主机 | 发布版 EXE 构建通过；NSIS 封装未完成 | 发布版窗口启动通过 | 资源清单测试通过；安装目录待验证 | 待验证     | 等待用户人工发布验收                 |
| 用户人工发布验收记录 | 待用户填写                           | 待用户填写         | 待用户填写                       | 待用户填写 | `phase-9-user-release-acceptance.md` |

只有用户在当前 Windows 10/11 主机完成安装、断网启动、资源存在和卸载的人工验收并留下记录后，OpenSpec 任务 1.5 才能标记完成。

## 当前阻塞

- Tauri 发布版应用已在低内存单作业模式构建成功。
- NSIS 封装需要首次从 Tauri 官方 GitHub Release 下载 `nsis-3.11.zip`；Tauri 下载和系统下载均因连接重置失败。
- 用户已决定不安装虚拟机；干净环境验收改由用户在当前 Windows 10/11 主机人工执行。安装包仍需在真实桌面完成安装、离线启动、托盘和卸载检查。

## 可重复步骤

1. 在当前 Windows 主机保存生成的 NSIS 安装程序和安装前版本信息。
2. 临时断开网络后运行安装程序，记录安装结果和安装目录。
3. 启动校园信箱，确认主窗口可打开且不要求登录或联网。
4. 确认安装目录内存在 `models/placeholder-model.txt` 和 `models/manifest.json`。
5. 关闭主窗口，确认进程仍驻留；通过托盘重新打开，再选择“彻底退出”。
6. 从 Windows“应用和功能”卸载，确认程序文件被移除，并记录用户数据保留策略的实际表现。完成后将结果填写到第九阶段发布验收记录。
